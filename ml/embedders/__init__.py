"""Text embedder implementations for the duplicate-retrieval benchmark.

The protocol these satisfy is declared alongside the other serving protocols;
the implementations live here because they carry heavy dependencies. Populated
in a later task — this package is created now so the import boundary can be
enforced before there is anything to enforce it against.
"""
